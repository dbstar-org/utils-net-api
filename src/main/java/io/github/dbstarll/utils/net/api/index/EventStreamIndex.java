package io.github.dbstarll.utils.net.api.index;

public class EventStreamIndex extends AbstractIndex<EventStream> {
    /**
     * 构建EventStreamIndex.
     *
     * @param data  EventStream
     * @param index index
     */
    public EventStreamIndex(final EventStream data, final int index) {
        super(data, index);
    }
}
