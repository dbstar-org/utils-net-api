package io.github.dbstarll.utils.net.api.index;

public final class StringIndex extends AbstractIndex<String> {
    /**
     * 构建StringIndex.
     *
     * @param data  string data
     * @param index index
     */
    public StringIndex(final String data, final int index) {
        super(data, index);
    }
}
